# Supabase Configuration
# Get these values from: https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Service Role Key (APENAS para Super Admin enviar convites)
# ATENÇÃO: Esta chave tem permissões TOTAIS no banco
# Use APENAS em ambiente local ou servidor seguro
# NUNCA comite esta chave no Git
VITE_SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# IMPORTANTE - SEGURANÇA:
# - A ANON_KEY é PÚBLICA e pode ser exposta no frontend
# - A SERVICE_ROLE_KEY é PRIVADA e só deve ser usada por super admins
# - Para produção, use variáveis de ambiente do servidor
# - A segurança real vem das políticas RLS no banco de dados

# Environment
VITE_APP_ENV=development

# URL da aplicação (usado para links em emails)
# Em desenvolvimento: http://localhost:5173
# Em produção: https://seu-dominio.com
VITE_APP_URL=http://localhost:5173
